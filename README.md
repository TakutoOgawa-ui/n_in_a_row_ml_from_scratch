# n_in_a_row_ml_from_scratch
N-in-a-row game with AI implemented from scratch in pure Python. The board size and winning condition (n) can be customized. Includes forward propagation (fprop) and backpropagation (bprop) without using external libraries like NumPy.
